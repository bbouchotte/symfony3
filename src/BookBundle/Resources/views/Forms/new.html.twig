{% extends 'BookBundle::base.html.twig' %}

{% block body_content %} %}

	{{ form_start(form, {'attr': {'novalidate': 'novalidate', 'class': 'form_control' }}) }}
	
	{% if task is defined %}
		{{ form.vars.value.task }}
	{% endif %}
	
	{% if personage is defined %}
		{{ form.vars.value.personage }}
	{% endif %}
	
	{{ form_errors(form) }}
	{{ form_widget(form) }}
	{{ form_end(form) }}
	
{% endblock %}